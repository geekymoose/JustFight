using UnityEngine;

namespace WeaponSystem
{
    [CreateAssetMenu(fileName = "DamageType", menuName = "ScriptableObjects/WeaponSystem/DamageType", order = 1)]
    public class DamageType : ScriptableObject
    {
        // Empty ScriptableObject.
        // Used as a flexible enum to list type of damage by designers
    }
}
